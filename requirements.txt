# Host-side Python dependencies for GALBA2.
#
# All bioinformatics tools (AUGUSTUS, DIAMOND, miniprot, miniprothint,
# compleasm, BUSCO, RepeatMasker, Red, OMArk, AGAT, gffcompare, barrnap,
# tRNAscan-SE, Infernal, FANTASIA-Lite, minisplice, etc.) run inside
# Singularity containers and are NOT tracked here. Their pinned image
# tags live in rules/common.smk and are documented in the README
# container table.
#
# Install with:
#   pip install -r requirements.txt

snakemake==8.18.2
snakemake-executor-plugin-slurm==2.6.0
pandas==2.0.2
